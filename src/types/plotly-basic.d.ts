// plotly.js-finance-dist is a CommonJS build of plotly.js (finance bundle).
// Declare it as `any` so it can be used as a runtime value.
// Types are imported directly from `plotly.js` in consuming files.
declare module "plotly.js-finance-dist";
