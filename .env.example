# Optional: self-hosted CORS proxy for the Yahoo Finance API (see worker/).
# When set, it is tried before the public proxies and its edge cache is shared
# by every viewer. Leave unset to rely on the public proxies only.
# VITE_YF_PROXY=https://yf-proxy.<your-subdomain>.workers.dev
